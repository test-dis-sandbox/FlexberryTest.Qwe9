//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useQuery } from '@tanstack/react-query';

import { ScheduleGroupService } from '@/services/ScheduleGroup.service';

const useGetCountScheduleGroup = (viewName: string) => {
  const { data, isLoading, isError } = useQuery({
    queryKey: ['scheduleGroup', 'count', viewName],
    retry: false,
    queryFn: async (): Promise<number> => {
      const response = await ScheduleGroupService.GetCount(viewName);
      return response.data;
    },
  });

  return {
    count: data || 0,
    isLoading,
    isError,
  };
};

export default useGetCountScheduleGroup;
