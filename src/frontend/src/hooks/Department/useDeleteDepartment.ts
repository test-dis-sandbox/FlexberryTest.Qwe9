//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { DepartmentService } from '@/services/Department.service';

const useDeleteDepartment = (onSuccess: (result: string) => void, onError: (e: Error | AxiosError) => void) => {
  const queryClient = useQueryClient();

  const {
    mutate: deleteDepartment,
    mutateAsync: deleteDepartmentAsync,
    isPending,
  } = useMutation({
    mutationFn: (id: string) => DepartmentService.DeleteDepartment(id),
    onSuccess: (_, id) => {
      const className = 'department';
      queryClient.invalidateQueries({ queryKey: [className, id] });
      queryClient.invalidateQueries({ queryKey: [className, 'list'] });
      queryClient.invalidateQueries({ queryKey: [className, 'count'] });
      onSuccess(id);
    },
    onError: (e) => onError(e),
  });

  return { deleteDepartment, deleteDepartmentAsync, isPending };
};

export default useDeleteDepartment;
