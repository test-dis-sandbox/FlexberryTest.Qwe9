//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { ScheduleService } from '@/services/Schedule.service';

const useDeleteAllSchedule = (onSuccess: (result: string[]) => void, onError: (e: Error | AxiosError) => void) => {
  const queryClient = useQueryClient();

  const {
    mutate: deleteAllSchedule,
    mutateAsync: deleteAllScheduleAsync,
    isPending,
  } = useMutation({
    mutationFn: (ids: string[]) => ScheduleService.DeleteAllSchedule(ids),
    onSuccess: (_, ids) => {
      const className = 'schedule';
      ids.forEach((id) => {
        queryClient.removeQueries({ queryKey: [className, id] });
      });
      queryClient.invalidateQueries({ queryKey: [className, 'list'] });
      queryClient.invalidateQueries({ queryKey: [className, 'count'] });
      onSuccess(ids);
    },
    onError: (e) => onError(e),
  });

  return { deleteAllSchedule, deleteAllScheduleAsync, isPending };
};

export default useDeleteAllSchedule;
