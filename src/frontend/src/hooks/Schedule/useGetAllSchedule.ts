//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useState } from 'react';
import { useQuery } from '@tanstack/react-query';

import { ScheduleService } from '@/services/Schedule.service';
import { IBaseSchedule } from '@/types/Schedule.types';
import { GetAllRequestResult } from '@/types/helpers/GetAllRequestResult';
import { GetAllRequestOptions } from '@/types/helpers/GetAllRequestOptions';

const useGetAllSchedule = <T extends IBaseSchedule>({
  viewName,
  perPage,
  page,
  sorting,
  filter,
  search,
  enabled = true,
}: GetAllRequestOptions) => {
  const [isTriggered, setIsTriggered] = useState(false);

  const { data, isFetching, isError } = useQuery({
    queryKey: ['schedule', 'list', viewName, { perPage, page, sorting, filter, search }],
    retry: false,
    enabled: isTriggered || enabled,
    placeholderData: (prev) => prev,
    queryFn: async (): Promise<GetAllRequestResult<T>> => {
      const response = await ScheduleService.GetAllSchedule(viewName, perPage, page, sorting, filter, search);
      return response.data;
    },
  });

  const trigger = () => {
    setIsTriggered(true);
  };

  return {
    data: data?.records || [],
    count: data?.count || 0,
    isLoading: isFetching,
    isError,
    trigger,
  };
};

export default useGetAllSchedule;
