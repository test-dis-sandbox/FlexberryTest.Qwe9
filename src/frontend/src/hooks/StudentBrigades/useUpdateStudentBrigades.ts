//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { StudentBrigadesService } from '@/services/StudentBrigades.service';
import { IBaseStudentBrigades } from '@/types/StudentBrigades.types';

const useUpdateStudentBrigades = <T extends IBaseStudentBrigades>(
  onSuccess: (result: T) => void,
  onError: (e: Error | AxiosError) => void
) => {
  const queryClient = useQueryClient();

  type UpdateRecordArgs = {
    studentBrigades: T;
    viewName: string;
  };

  const {
    mutate: updateStudentBrigades,
    mutateAsync: updateStudentBrigadesAsync,
    isPending,
  } = useMutation({
    mutationFn: (updateRecordArgs: UpdateRecordArgs) =>
      StudentBrigadesService.UpdateStudentBrigades<T>(updateRecordArgs.studentBrigades, updateRecordArgs.viewName),
    onSuccess: (response, variables) => {
      const id = response.data.id;
      const { viewName } = variables;
      const className = 'studentBrigades';
      queryClient.invalidateQueries({ queryKey: [className, id] });
      queryClient.invalidateQueries({ queryKey: [className, 'list', viewName] });
      onSuccess(response.data);
    },
    onError: (e) => onError(e),
  });

  return { updateStudentBrigades, updateStudentBrigadesAsync, isPending };
};

export default useUpdateStudentBrigades;
