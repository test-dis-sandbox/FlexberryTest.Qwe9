//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { StudyPlanService } from '@/services/StudyPlan.service';

const useDeleteAllStudyPlan = (onSuccess: (result: string[]) => void, onError: (e: Error | AxiosError) => void) => {
  const queryClient = useQueryClient();

  const {
    mutate: deleteAllStudyPlan,
    mutateAsync: deleteAllStudyPlanAsync,
    isPending,
  } = useMutation({
    mutationFn: (ids: string[]) => StudyPlanService.DeleteAllStudyPlan(ids),
    onSuccess: (_, ids) => {
      const className = 'studyPlan';
      ids.forEach((id) => {
        queryClient.removeQueries({ queryKey: [className, id] });
      });
      queryClient.invalidateQueries({ queryKey: [className, 'list'] });
      queryClient.invalidateQueries({ queryKey: [className, 'count'] });
      onSuccess(ids);
    },
    onError: (e) => onError(e),
  });

  return { deleteAllStudyPlan, deleteAllStudyPlanAsync, isPending };
};

export default useDeleteAllStudyPlan;
