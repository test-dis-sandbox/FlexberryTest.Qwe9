import { styled } from '@mui/material/styles';
import { Paper } from '@mui/material';

const MainPaper = styled(Paper)({
  padding: '20px',
  textAlign: 'center',
  height: '100%',
});

export { MainPaper };
