//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { DataTableFilterMeta, DataTableSortMeta } from 'primereact/datatable';

import axiosInstance from '@/api/instance';
import { IBaseAudience } from '@/types/Audience.types';
import { getQueryString } from '@/utils/queryStringConverter';
import { SearchColumnsParam } from '@/types/helpers/SearchColumnsParam';

// *** Start programmer edit section *** (Import statements)

// *** End programmer edit section *** (Import statements)

export const AudienceService = {
  async GetAllAudience(
    viewName: string,
    perPage?: number,
    page?: number,
    sorting?: DataTableSortMeta[],
    filter?: DataTableFilterMeta,
    search?: SearchColumnsParam
  ) {
    const queryStr = getQueryString(perPage, page, sorting, filter, search);
    return await axiosInstance.get(`/audience/${viewName}${queryStr}`);
  },

  async GetAudience(id: string, viewName: string) {
    return await axiosInstance.get(`/audience/${viewName}/${id}`);
  },

  async CreateAudience<T extends IBaseAudience>(data: T, viewName: string) {
    const response = await axiosInstance.post(`/audience/${viewName}`, data, {
      headers: {
        'Content-Type': 'application/json',
      },
    });

    return response;
  },

  async UpdateAudience<T extends IBaseAudience>(data: T, viewName: string) {
    const response = await axiosInstance.put(`/audience/${viewName}`, data, {
      headers: {
        'Content-Type': 'application/json',
      },
    });

    return response;
  },

  async DeleteAudience(id: string) {
    return await axiosInstance.delete(`/audience/${id}`);
  },

  async DeleteAllAudience(ids: string[]) {
    return await axiosInstance.post(`/audience/DeleteAll`, ids, {
      headers: {
        'Content-Type': 'application/json',
      },
    });
  },

  async GetCount(viewName: string) {
    return await axiosInstance.get(`/audience/${viewName}/count`);
  },

  // *** Start programmer edit section *** (Custom methods)

  // *** End programmer edit section *** (Custom methods)
};
