# SQL-миграции

Положите сюда сгенерированные SQL создания схемы БД (напр. результат flexberry с PostgreSql.FullSql=true).

Liquibase применит все `.sql` из этой папки через `changelog.xml` (`<includeAll path="migrations"/>`).
