//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
namespace Qwe.ApplicationLayer.DTO.Group
{
    using System;
    using ICSSoft.STORMNET;
    using Qwe;
    using Qwe.ApplicationLayer;
    using Qwe.ApplicationLayer.Attributes;
    using Qwe.ApplicationLayer.Helpers;

    // *** Start programmer edit section *** (Using statements)

    // *** End programmer edit section *** (Using statements)

    /// <summary>
    /// DTO для представления Group.GroupE.
    /// </summary>
    [DataView(typeof(Group), "GroupE")]
    public partial class GroupEDto : GroupDtoBase
    {
        /// <summary>
        /// Инициализирует новый экземпляр <see cref="GroupEDto"/>.
        /// </summary>
        public GroupEDto()
        {
        }

        /// <summary>
        /// Инициализирует новый экземпляр <see cref="GroupEDto"/>.
        /// </summary>
        /// <param name="source">Объект для заполнения значений.</param>
        public GroupEDto(Group source)
        {
            FillFromClass(source);
        }

                /// <summary>
        /// Название.
        /// </summary>
        [PropertyName("Name")]
        [CaptionProperty("Название")]
        public string Name { get; set; } = string.Empty;

        /// <summary>
        /// Год поступления.
        /// </summary>
        [PropertyName("EnrollmentYear")]
        [CaptionProperty("Год поступления")]
        public DateTime EnrollmentYear { get; set; } = DateTime.Now;

        /// <summary>
        /// Форма обучения.
        /// </summary>
        [PropertyName("Form")]
        [CaptionProperty("Форма обучения")]
        public tEducationForm Form { get; set; }

        /// <summary>
        /// Магистратура.
        /// </summary>
        [PropertyName("IsMaster")]
        [CaptionProperty("Магистратура")]
        public bool IsMaster { get; set; } = false;

                /// <summary>
        /// Полное название группы.
        /// </summary>
        [PropertyName("FullName")]
        [CaptionProperty("Полное название группы")]
        public string FullName { get; set; } = string.Empty;

        /// <summary>
        /// Кафедра.
        /// </summary>
        [PropertyName("Department")]
        [CaptionProperty("Кафедра")]
        public Guid DepartmentId { get; set; }

        /// <summary>
        /// Староста.
        /// </summary>
        [PropertyName("Leader")]
        [CaptionProperty("Староста")]
        public Guid LeaderId { get; set; }

        /// <summary>
        /// ФИО старосты.
        /// </summary>
        [PropertyName("Leader.FullName")]
        [CaptionProperty("ФИО старосты")]
        public string? LeaderFullName { get; set; }

        /// <summary>
        /// Помощник старосты.
        /// </summary>
        [PropertyName("Assistant")]
        [CaptionProperty("Помощник старосты")]
        public Guid? AssistantId { get; set; }

        /// <summary>
        /// ФИО помощника старосты.
        /// </summary>
        [PropertyName("Assistant.FullName")]
        [CaptionProperty("ФИО помощника старосты")]
        public string? AssistantFullName { get; set; }

        // *** Start programmer edit section *** (Custom properties)

        // *** End programmer edit section *** (Custom properties)

        // *** Start programmer edit section *** (Custom details)

        // *** End programmer edit section *** (Custom details)

        /// <inheritdoc/>
        public override GroupEDto FillFromClass(Group source)
        {
            if (source == null)
            {
                throw new ArgumentNullException(nameof(source));
            }

            Id = PKHelper.GetGuidByObject(source).GetValueOrDefault();
            Name = source.Name;
            EnrollmentYear = source.EnrollmentYear.ToUniversalTime();
            Form = source.Form;
            IsMaster = source.IsMaster;
            FullName = source.FullName;
            DepartmentId = PKHelper.GetGuidByObject(source.Department).GetValueOrDefault();
            LeaderId = PKHelper.GetGuidByObject(source.Leader).GetValueOrDefault();
            LeaderFullName = source.Leader?.FullName ?? string.Empty;
            AssistantId = PKHelper.GetGuidByObject(source.Assistant);
            AssistantFullName = source.Assistant?.FullName ?? string.Empty;

            // *** Start programmer edit section *** (FillFromClass custom)

            // *** End programmer edit section *** (FillFromClass custom)

            return this;
        }

        /// <inheritdoc/>
        public override void UpdateFromDto(Group destination)
        {
            if (destination == null)
            {
                throw new ArgumentNullException(nameof(destination));
            }

            destination.__PrimaryKey = Id;
            destination.Name = Name;
            destination.EnrollmentYear = EnrollmentYear.ToUniversalTime();
            destination.Form = Form;
            destination.IsMaster = IsMaster;
            destination.FullName = FullName;

            if (DepartmentId != Guid.Empty)
            {
                Department department = new Department();
                department.SetExistObjectPrimaryKey(DepartmentId);
                destination.Department = department;
            }
            else
            {
                destination.Department = null;
            }

            if (LeaderId != Guid.Empty)
            {
                Student leader = new Student();
                leader.SetExistObjectPrimaryKey(LeaderId);
                destination.Leader = leader;
            }
            else
            {
                destination.Leader = null;
            }

            if (AssistantId.HasValue)
            {
                Student assistant = new Student();
                assistant.SetExistObjectPrimaryKey(AssistantId);
                destination.Assistant = assistant;
            }
            else
            {
                destination.Assistant = null;
            }

            // *** Start programmer edit section *** (UpdateFromDto custom)

            // *** End programmer edit section *** (UpdateFromDto custom)
        }
    }
}
