//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
namespace Qwe.ApplicationLayer.DTO.Student
{
    using System;
    using ICSSoft.STORMNET;
    using Qwe;
    using Qwe.ApplicationLayer;
    using Qwe.ApplicationLayer.Attributes;
    using Qwe.ApplicationLayer.Helpers;

    // *** Start programmer edit section *** (Using statements)

    // *** End programmer edit section *** (Using statements)

    /// <summary>
    /// DTO для представления Student.StudentE.
    /// </summary>
    [DataView(typeof(Student), "StudentE")]
    public partial class StudentEDto : StudentDtoBase
    {
        /// <summary>
        /// Инициализирует новый экземпляр <see cref="StudentEDto"/>.
        /// </summary>
        public StudentEDto()
        {
        }

        /// <summary>
        /// Инициализирует новый экземпляр <see cref="StudentEDto"/>.
        /// </summary>
        /// <param name="source">Объект для заполнения значений.</param>
        public StudentEDto(Student source)
        {
            FillFromClass(source);
        }

        /// <summary>
        /// Дата зачисления.
        /// </summary>
        [PropertyName("EnrollmentDate")]
        [CaptionProperty("Дата зачисления")]
        public DateTime EnrollmentDate { get; set; } = DateTime.Now;

                /// <summary>
        /// ФИО.
        /// </summary>
        [PropertyName("FullName")]
        [CaptionProperty("ФИО")]
        public string FullName { get; set; } = string.Empty;

        /// <summary>
        /// Бюджетная основа.
        /// </summary>
        [PropertyName("IsBudget")]
        [CaptionProperty("Бюджетная основа")]
        public bool IsBudget { get; set; } = false;

        /// <summary>
        /// Статус.
        /// </summary>
        [PropertyName("Status")]
        [CaptionProperty("Статус")]
        public tStudentStatus Status { get; set; }

                /// <summary>
        /// Номер зачётной книжки.
        /// </summary>
        [PropertyName("StudentId")]
        [CaptionProperty("Номер зачётной книжки")]
        public string StudentId { get; set; } = string.Empty;

        /// <summary>
        /// Преподаватель.
        /// </summary>
        [PropertyName("Teacher")]
        [CaptionProperty("Преподаватель")]
        public Guid TeacherId { get; set; }

        /// <summary>
        /// Учебный план.
        /// </summary>
        [PropertyName("Plan")]
        [CaptionProperty("Учебный план")]
        public Guid PlanId { get; set; }

        /// <summary>
        /// Группа.
        /// </summary>
        [PropertyName("Group")]
        [CaptionProperty("Группа")]
        public Guid? GroupId { get; set; }

        /// <summary>
        /// Название группы.
        /// </summary>
        [PropertyName("Group.Name")]
        [CaptionProperty("Название группы")]
        public string? GroupName { get; set; }

        // *** Start programmer edit section *** (Custom properties)

        // *** End programmer edit section *** (Custom properties)

        // *** Start programmer edit section *** (Custom details)

        // *** End programmer edit section *** (Custom details)

        /// <inheritdoc/>
        public override StudentEDto FillFromClass(Student source)
        {
            if (source == null)
            {
                throw new ArgumentNullException(nameof(source));
            }

            Id = PKHelper.GetGuidByObject(source).GetValueOrDefault();
            EnrollmentDate = source.EnrollmentDate.ToUniversalTime();
            FullName = source.FullName;
            IsBudget = source.IsBudget;
            Status = source.Status;
            StudentId = source.StudentId;
            TeacherId = PKHelper.GetGuidByObject(source.Teacher).GetValueOrDefault();
            PlanId = PKHelper.GetGuidByObject(source.Plan).GetValueOrDefault();
            GroupId = PKHelper.GetGuidByObject(source.Group);
            GroupName = source.Group?.Name ?? string.Empty;

            // *** Start programmer edit section *** (FillFromClass custom)

            // *** End programmer edit section *** (FillFromClass custom)

            return this;
        }

        /// <inheritdoc/>
        public override void UpdateFromDto(Student destination)
        {
            if (destination == null)
            {
                throw new ArgumentNullException(nameof(destination));
            }

            destination.__PrimaryKey = Id;
            destination.EnrollmentDate = EnrollmentDate.ToUniversalTime();
            destination.FullName = FullName;
            destination.IsBudget = IsBudget;
            destination.Status = Status;
            destination.StudentId = StudentId;

            if (TeacherId != Guid.Empty)
            {
                Teacher teacher = new Teacher();
                teacher.SetExistObjectPrimaryKey(TeacherId);
                destination.Teacher = teacher;
            }
            else
            {
                destination.Teacher = null;
            }

            if (PlanId != Guid.Empty)
            {
                StudyPlan plan = new StudyPlan();
                plan.SetExistObjectPrimaryKey(PlanId);
                destination.Plan = plan;
            }
            else
            {
                destination.Plan = null;
            }

            if (GroupId.HasValue)
            {
                Group group = new Group();
                group.SetExistObjectPrimaryKey(GroupId);
                destination.Group = group;
            }
            else
            {
                destination.Group = null;
            }

            // *** Start programmer edit section *** (UpdateFromDto custom)

            // *** End programmer edit section *** (UpdateFromDto custom)
        }
    }
}
